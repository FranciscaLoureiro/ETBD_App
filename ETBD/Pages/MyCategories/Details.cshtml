@page
@model ETBD.Pages.MyCategories.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Category Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div>
<hr />
<div>
    <a asp-page="./Edit" class="btn btn-primary" asp-route-id="@Model.Category.Id">Edit</a> |
    <a asp-page="./Index" class="btn btn-secondary">Back</a>
</div>
